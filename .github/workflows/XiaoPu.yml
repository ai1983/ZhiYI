name:  xp笑谱
on:
  workflow_dispatch:
  schedule:
     - cron: '0,30 0-10 * * *'
  watch:
    types: started
jobs:
  build:
    runs-on: ubuntu-latest
    if: github.event.repository.owner.id == github.event.sender.id

    steps:
      - name: Checkout
        run: |
          git clone https://github.com/love-pig-pig/Daily-tasks.git ~/Daily-tasks
      - name: Use Node.js 12.x
        uses: actions/setup-node@v1
        with:
          node-version: 12.x
      - name: npm install
        run: |
          cd ~/Daily-tasks
          git checkout master
          npm install
      - name: '运行 【笑谱】'
        run: |
          cd ~/Daily-tasks
          node Task/iboxpay.js
        env:
             XP_refreshTOKEN: ${{ secrets.XP_refreshTOKEN }}
             XP_CASH: ${{ secrets.XP_CASH }} //提现金额
             XP_live: ${{ secrets.XP_live }} //直播
             XP_phone: ${{ secrets.XP_phone }} //手机号码
             XP_sms: ${{ secrets.XP_sms }} //验证码
             TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
             TG_USER_ID: ${{ secrets.TG_USER_ID }}

